---
---

<section class="newsletter-section">
		<h3>Descubre nuestras novedades</h3>
		<div class="newsletter-card">
			<div class="newsletter-left">
				<h4>¿Quieres conocer nuestras novedades?</h4>
				<p>Introduce tu email y recibirás novedades del Centro de Formación Fyrescan</p>
			</div>
			<div class="newsletter-right">
				<input type="email" placeholder="Tu email aquí" aria-label="Email" />
				<button class="btn-sub">✉ Suscribirse</button>
			</div>
		</div>
	</section>
    <style>
        /* Desktop: 1025px+ */
        .newsletter-section{ padding: 2.5rem 1rem; background: #fff; width: 100%; }
        .newsletter-section h3{ text-align:center; color: #2f6a77; font-family:'Outfit',sans-serif; margin-bottom:1.4rem; font-size: 1.8rem; }
        .newsletter-card{ max-width:1100px; margin:0 auto; background:#fff; padding:1.5rem; border-radius:8px; box-shadow:0 8px 28px rgba(10,20,30,0.06); display:flex; gap:1rem; align-items:center; }
        .newsletter-left{ flex:1 }
        .newsletter-left h4 { font-family: 'Outfit', sans-serif; font-size: 1.4rem; margin: 0 0 0.5rem 0; }
        .newsletter-left p { font-family: 'Quicksand', sans-serif; font-size: 1rem; margin: 0; color: #666; }
        .newsletter-right{ display:flex; gap:.75rem; align-items:center }
        .newsletter-right input{ padding:.8rem 1rem; border-radius:999px; border:2px solid #cfe3ea; min-width:200px; font-size: 0.95rem; }
        .btn-sub{ background:#2f6a77; color:white; border:none; padding:.7rem 1.1rem; border-radius:999px; cursor:pointer; font-weight: 600; }

        /* Tablet: 768px - 1024px */
        @media (min-width: 768px) and (max-width: 1024px){
            .newsletter-section{ padding: 2rem 0.8rem; width: 100vw; min-width: 0; box-sizing: border-box; }
            .newsletter-section h3{ margin-bottom: 1.2rem; font-size: 1.5rem; }
            .newsletter-card{ flex-direction: column; align-items: stretch; padding: 1.2rem; gap: 1rem; }
            .newsletter-left h4 { font-size: 1.2rem; }
            .newsletter-left p { font-size: 0.95rem; }
            .newsletter-right{ flex-direction: column; justify-content: center; }
            .newsletter-right input{ min-width: 100%; }
            .btn-sub{ width: 100%; padding: 0.7rem 1rem; }
        }

        /* Mobile large (phablet): 600px - 767px */
        @media (min-width: 600px) and (max-width: 767px){
            .newsletter-section{ padding: 1.5rem 0.6rem; width: 100vw; min-width: 0; box-sizing: border-box; }
            .newsletter-section h3{ margin-bottom: 1rem; font-size: 1.3rem; }
            .newsletter-card{ flex-direction: column; align-items: stretch; padding: 1rem; gap: 0.8rem; }
            .newsletter-left h4 { font-size: 1.1rem; }
            .newsletter-left p { font-size: 0.9rem; }
            .newsletter-right{ flex-direction: column; justify-content: center; }
            .newsletter-right input{ min-width: 100%; padding: 0.7rem 0.9rem; font-size: 0.9rem; }
            .btn-sub{ width: 100%; padding: 0.6rem 0.9rem; font-size: 0.95rem; }
        }

        /* Mobile small: 375px - 599px */
        @media (min-width: 375px) and (max-width: 599px){
            .newsletter-section{ padding: 1.2rem 0.5rem; width: 100vw; min-width: 0; box-sizing: border-box; }
            .newsletter-section h3{ margin-bottom: 0.8rem; font-size: 1.1rem; }
            .newsletter-card{ flex-direction: column; align-items: stretch; padding: 0.8rem; gap: 0.6rem; }
            .newsletter-left h4 { font-size: 0.95rem; margin: 0 0 0.3rem 0; }
            .newsletter-left p { font-size: 0.85rem; }
            .newsletter-right{ flex-direction: column; justify-content: center; gap: 0.5rem; }
            .newsletter-right input{ min-width: 100%; padding: 0.6rem 0.8rem; font-size: 0.85rem; }
            .btn-sub{ width: 100%; padding: 0.55rem 0.8rem; font-size: 0.9rem; }
        }
    </style>
