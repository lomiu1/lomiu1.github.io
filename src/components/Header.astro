---
import logo from '../assets/LOGO.png';
---

    <header class="font-header">
        <nav class="nav">
            <div class="nav-left">
                        <a class="logo-link" href="/inicio">
                            <img class="logo" src={logo.src} alt="Logo Fyrescan" />
                        </a>
            </div>


            <div class="nav-right">
                <button id="menuBtn" class="menu-btn" aria-label="Abrir menú">☰</button>
                <ul class="nav-links">
                    <li><a href="/inicio">Inicio</a></li>
                    <li><a href="/Curso">Curso</a></li>
                    <li><a href="/blog">Blog</a></li>
                    <li><a href="/contacto">Contacto</a></li>
                </ul>
            </div>
        </nav>

        <!-- Menú móvil desplegable -->
        <div id="mobileMenu" class="mobile-menu" hidden>
            <ul>
                <li><a href="/inicio">Inicio</a></li>
                <li><a href="/Curso">Curso</a></li>
                <li><a href="/blog">Blog</a></li>
                <li><a href="/contacto">Contacto</a></li>
            </ul>
        </div>
    </header>

    <style>
        /* Import Outfit from Google Fonts; adjust weights as needed */
        @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;700;800&display=swap');

        /*
          Clase siguiendo tu patrón: .outfit-<uniquifier>
          Aquí uso .outfit-header; si quieres otro identificador cambia el sufijo.
        */
                    .font-header {
                            font-family: "Outfit", sans-serif;
                            font-optical-sizing: auto;
                            font-weight: 700; /* cambia a 400/700 según prefieras */
                            font-style: normal;
                            font-size: 16px;
                            min-width: 375px;
                    }

        :root{
            --bg: #cae7fbbf;
            --accent: #074d7c;
            --muted: #57a6dc;
            --white: #4b78ea;
            --border: #c3d9ef83; /* Azul oscuro para el borde */
        }

        /* Force fixed header so it does not move on scroll. Use high z-index and !important
           in case other styles interfere. */
            /* Make header sticky so it remains visible at the top when scrolling
               but still occupies space in the document flow (so page content is
               not positioned underneath it). Keep a solid background so content
               below doesn't show through. */
            .font-header {
                background: var(--bg);
                border-top: 4px solid var(--border);
                border-bottom: 4px solid var(--border);
                position:relative;
                top: 0;
                z-index: 9999;
                width: 100%;
                margin: 0;
                /* ensure the header paints its own stacking context */
                -webkit-backface-visibility: hidden;
                backface-visibility: hidden;
                /* subtle shadow so it's visually separated */
                box-shadow: 0 2px 8px rgba(10,10,10,0.06);
            }

        .nav{
            display: flex;
            align-items: center;
            justify-content: space-between;
            max-width: 1400px;
            margin: 0 auto;
            padding: 0.5rem;
            height: 70px;
            width: 100%;
            min-width: 0;
            box-sizing: border-box;
        }

        .nav-left, .nav-right{
            display: flex;
            align-items: center;
        }

            .logo{
                /* Constrain el logo para que no desborde la altura de la barra */
                height: 56px;
                width: auto;
                object-fit: contain;
            }


        .nav-links{
            list-style: none;
            display: flex;
            gap: 2rem;
            margin: 0;
            padding: 0;
        }

        .nav-links a{
            color: var(--muted);
            text-decoration: none;
            padding: 0.5rem;
            transition: all 0.3s ease;
            font-weight: 500;
            font-size: 1.2rem;
            position: relative;
        }

        .nav-links a:after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: 0;
            left: 0;
            background-color: var(--accent);
            transition: width 0.3s ease;
        }

        .nav-links a:hover {
            color: var(--accent);
        }

        .nav-links a:hover:after {
            width: 100%;
        }

        .menu-btn{
            display: none;
            background: transparent;
            border: none;
            color: var(--white);
            font-size: 1.25rem;
            cursor: pointer;
            margin-right: 0.5rem;
        }

        /* Desktop: 1025px+ */
        @media (min-width: 1025px){
            .font-header { width: 100%; }
            .nav { width: 100%; max-width: 1400px; }
            .nav-links{ display: flex; }
            .menu-btn{ display: none; }
            .logo{ height: 56px; width: auto; }
            .nav-links a{ font-size: 1.2rem; padding: 0.5rem; }
        }

        /* Tablet: 768px - 1024px */
        @media (min-width: 768px) and (max-width: 1024px){
            .font-header { width: 100vw; min-width: 0; }
            .nav { width: 100vw; min-width: 0; padding: 0.4rem 0.5rem; }
            .nav-links{ display: flex; gap: 1.5rem; }
            .menu-btn{ display: none; }
            .logo{ height: 50px; width: auto; }
            .nav-links a{ font-size: 1rem; padding: 0.4rem 0.4rem; }
            .mobile-menu{ display: none; }
        }

        /* Mobile large (phablet): 600px - 767px */
        @media (min-width: 600px) and (max-width: 767px){
            .font-header { width: 100vw; min-width: 0; }
            .nav { width: 100vw; min-width: 0; padding: 0.3rem 0.5rem; height: 60px; }
            .nav-left{ order: 1; }
            .nav-right{ order: 3; }
            .nav-links{ display: none; }
            .menu-btn{ display: inline-flex; font-size: 1.1rem; margin-right: 0.3rem; }
            .logo{ height: 45px; width: auto; }
            .mobile-menu{ display: none; background: var(--bg); padding: 0.4rem 0.8rem; border-top: 2px solid var(--border); }
            .mobile-menu.open{ display: block; }
            .mobile-menu ul{ list-style:none; padding:0; margin:0; display:flex; flex-direction:column; gap:0.15rem; }
            .mobile-menu a{ color: var(--white); text-decoration:none; padding:0.5rem 0.2rem; display:block; font-size: 0.95rem; font-weight: 500; }
            .mobile-menu a:hover{ color: var(--accent); }
        }

        /* Mobile small: 375px - 599px */
        @media (min-width: 375px) and (max-width: 599px){
            .font-header { width: 100vw; min-width: 0; box-sizing: border-box; }
            .nav { width: 100vw; min-width: 0; padding: 0.25rem 0.4rem; height: 55px; box-sizing: border-box; }
            .nav-left{ order: 1; }
            .nav-right{ order: 3; }
            .nav-links{ display: none; }
            .menu-btn{ display: inline-flex; font-size: 1.05rem; margin-right: 0.2rem; }
            .logo{ height: 40px; width: auto; }
            .mobile-menu{ display: none; background: var(--bg); padding: 0.3rem 0.6rem; border-top: 2px solid var(--border); }
            .mobile-menu.open{ display: block; }
            .mobile-menu ul{ list-style:none; padding:0; margin:0; display:flex; flex-direction:column; gap:0.1rem; }
            .mobile-menu a{ color: var(--white); text-decoration:none; padding:0.45rem 0.15rem; display:block; font-size: 0.9rem; font-weight: 500; }
            .mobile-menu a:hover{ color: var(--accent); }
        }
    </style>

    <script>
        (function(){
            const btn = document.getElementById('menuBtn');
            const mobile = document.getElementById('mobileMenu');
            if(!btn || !mobile) return;
            btn.addEventListener('click', ()=>{
                const isOpen = mobile.classList.toggle('open');
                if(isOpen){
                    mobile.hidden = false;
                    btn.setAttribute('aria-expanded', 'true');
                } else {
                    mobile.hidden = true;
                    btn.setAttribute('aria-expanded', 'false');
                }
            });
        })();
    </script>